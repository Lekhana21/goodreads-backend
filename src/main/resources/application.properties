# Application name
spring.application.name=book-search-backend

# JDBC URL
spring.datasource.url=jdbc:mysql://shortline.proxy.rlwy.net:48937/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Credentials
spring.datasource.username=root
spring.datasource.password=dzkUScosfKsxNBNgwQAWIAvVaNVmoBVt

# Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Explicitly set dialect to avoid Dialect issues
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
